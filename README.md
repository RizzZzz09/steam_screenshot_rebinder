# Steam Screenshot Rebinder

⚡ Лёгкий инструмент для повторной загрузки скриншотов в Steam.  
Используется, если при загрузке в профиль нарушается порядок или Steam блокирует повторную отправку файлов с тем же
именем.

---

## 🎯 Что умеет программа

- 📂 Сканирует папки **OLD** (твои оригинальные скриншоты) и **NEW** (папка `screenshots` Steam).
- 🔄 Строит пары файлов `(OLD → NEW)` без сортировок — строго как в папке.
- 🖼 Подменяет содержимое файлов, сохраняя имена Steam.
- 🎛 Лёгкий интерфейс на PySide6.
- 📝 Красивый лог (с цветами, статусами, временем).
- 🧪 Dry-run режим (тестирование без изменений).
- ⚡ Поддержка JPG/PNG (или авто).
- ⌨️ Автоскрин (эмуляция F12):
    - задаёшь количество скринов и интервал,
    - задержка старта (чтобы успеть вернуться в игру),
    - лог с каждым нажатием и финальной сводкой.

---

## 📘 Как это работает

1. Копируешь свои **старые скриншоты** в папку `OLD`.
2. В Steam делаешь **новые скриншоты** (через F12) → они сохраняются в папку `NEW`.
3. Программа строит пары `(OLD → NEW)`.
4. Содержимое новых скринов заменяется на старые, но имена остаются как у новых.
5. Теперь их снова можно загружать в профиль 🚀

---

## 📁 Структура проекта

```
steam_screenshot_rebinder/
│
├── core/                   # Логика
│   ├── __init__.py
│   ├── autoscreen.py       # автоскрин (эмуляция F12)
│   ├── mapping.py          # сопоставление пар
│   ├── replacer.py         # замена содержимого
│   └── scanner.py          # сканирование папок OLD/NEW
│
├── ui/                     # PySide6 интерфейс
│   ├── __init__.py
│   └── main_window.py
│
├── tests/                  # pytest-тесты
│   ├── __init__.py
│   ├── test_mapping.py
│   ├── test_replacer.py
│   ├── test_scanner.py
│   └── test_scanner_ignore_thumbnails.py
│
├── app.py                  # Точка входа (GUI)
├── requirements.txt        # Зависимости (prod)
├── .gitignore              # Игнорируемые файлы Git
└── README.md               # Документация
```

## ▶️ Как запустить

1. Установи [Python 3.11+](https://www.python.org/downloads/)  
   (при установке поставь галочку **Add Python to PATH**).
2. Скачай ZIP-архив проекта (кнопка **Code → Download ZIP** на GitHub) и распакуй.
3. Запусти файл `app.py` (двойным кликом или через консоль: `python app.py`).
4. Для удобства можно создать `run.bat` рядом с `app.py`:
   ```bat
   @echo off
   python app.py
   pause
   ```
   Теперь запуск будет одним кликом.

---

## ▶️ Как пользоваться

1. Выбери папку **OLD** со старыми скриншотами.
2. Выбери папку **NEW** (из Steam).
3. Нажми **«Предпросмотр пар»** — увидишь список OLD → NEW.
4. Нажми **«Заменить содержимое»**:
    - если включён `Dry-run` → только лог;
    - если выключен → замена реально произойдёт в папке NEW.
5. Зайди в Steam и загрузи скриншоты в профиль.

---

## ▶️ Автоскрин (F12)

1. Укажи:
    - количество скринов,
    - интервал (сек),
    - задержку старта (сек).
2. Нажми **«Старт F12»** → программа будет нажимать F12 автоматически.
3. В логе отобразятся события:
   ```
   [12:34:56] [F12] скрин #1
   [12:34:58] [F12] скрин #2
   ...
   Автоскрин завершён: 10/10 сделано
   ```
4. Кнопка **«Стоп»** останавливает процесс в любой момент.

---

## ✅ Критерии готовности

- Программа запускается на Windows 11.
- Можно выбрать папки OLD/NEW.
- Есть предпросмотр и предупреждения.
- Замена работает (с логом).
- Dry-run ничего не меняет.
- Автоскрин работает (с отсчётом, логом, стоп-кнопкой).
- UI отзывчивый и без зависаний.

---

## 🛟 Устранение неполадок

- **Нет пар** → проверь, что папки выбраны правильно и не `thumbnails/`.
- **Файлы не меняются** → отключи галочку `Dry-run`.
- **Пустой предпросмотр** → в папках должны быть картинки.
- **Автоскрин не сработал** → убедись, что окно игры активно и Steam принимает F12.
