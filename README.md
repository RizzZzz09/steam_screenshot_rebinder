# Steam Screenshot Rebinder

⚡ Лёгкий desktop-инструмент для повторной загрузки скриншотов в Steam.  
Используется, если при загрузке в профиль нарушается порядок или Steam блокирует повторную отправку файлов с тем же именем.

Проект распространяется в виде **готового portable-приложения для Windows** и не требует установленного Python.

---

## 🎯 Что умеет программа

- 📂 Сканирует папки **OLD** (твои оригинальные скриншоты) и **NEW** (папка `screenshots` Steam).
- 🔄 Строит пары файлов `(OLD → NEW)` без сортировок — строго как в папке.
- 🖼 Подменяет содержимое файлов, сохраняя имена Steam.
- 🎛 Лёгкий интерфейс на PySide6.
- 📝 Подробный лог (цвета, статусы, время).
- 🧪 Dry-run режим (тестирование без изменений).
- ⚡ Поддержка JPG / PNG (или авто).
- ⌨️ Автоскрин (эмуляция F12):
  - задание количества и интервала,
  - задержка старта,
  - лог каждого нажатия и финальная сводка.

---

## 📘 Как это работает

1. Копируешь свои **старые скриншоты** в папку `OLD`.
2. В Steam делаешь **новые скриншоты** (через F12) → они сохраняются в папку `NEW`.
3. Программа строит пары `(OLD → NEW)`.
4. Содержимое новых скринов заменяется на старые, но имена остаются как у новых.
5. Теперь их снова можно загружать в профиль 🚀

---

## ▶️ Как использовать (рекомендуется)

### Готовое приложение (portable)

1. Перейди в раздел **Releases** на GitHub.
2. Скачай архив `SteamScreenshotRebinder-portable.zip`.
3. Распакуй архив.
4. Запусти `SteamScreenshotRebinder.exe`.

Python и зависимости **не требуются**.

---

## 🛠️ Запуск из исходников

### Требования
- Windows 10 / 11
- Python **3.11+**

### Установка runtime-зависимостей
```bash
pip install -r requirements.txt
```

### Запуск
```bash
python app.py
```

---

## 📦 Сборка portable-приложения (PyInstaller)

### Установка build-зависимостей
```bash
pip install -r requirements-dev.txt
```

### Сборка
```bash
pyinstaller SteamScreenshotRebinder.spec
```

Готовое приложение появится в папке `dist/`.

---

## 📁 Структура проекта

```
steam_screenshot_rebinder/
│
├── core/                   # Логика
├── ui/                     # PySide6 интерфейс
├── tests/                  # pytest-тесты
│
├── app.py                  # Точка входа (GUI)
├── requirements.txt        # runtime-зависимости
├── requirements-dev.txt    # build-зависимости
├── SteamScreenshotRebinder.spec
├── .gitignore
└── README.md
```

---

## ▶️ Как пользоваться

1. Выбери папку **OLD** со старыми скриншоты.
2. Выбери папку **NEW** (из Steam).
3. Нажми **«Предпросмотр пар»**.
4. Нажми **«Заменить содержимое»**:
   - `Dry-run` включён → только лог;
   - выключен → файлы реально заменяются.
5. Загрузи скриншоты в профиль Steam.

---

## ▶️ Автоскрин (F12)

1. Укажи:
   - количество скринов,
   - интервал,
   - задержку старта.
2. Нажми **«Старт F12»**.
3. В логе отобразятся события каждого нажатия.
4. Кнопка **«Стоп»** останавливает процесс в любой момент.

---

## 🛟 Устранение неполадок

- **Нет пар** → проверь папки и исключи `thumbnails/`.
- **Файлы не меняются** → отключи `Dry-run`.
- **Пустой предпросмотр** → в папках должны быть изображения.
- **Автоскрин не сработал** → окно игры должно быть активно, Steam должен принимать F12.

---

## ⚠️ Примечание

Приложение использует автоматизацию ввода (эмуляцию клавиш).  
Некоторые антивирусы могут реагировать на это — это ожидаемое поведение.
